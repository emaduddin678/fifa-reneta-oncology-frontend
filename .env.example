# Endpoint 3 — this frontend's own backend (accounts, SSO exchange, quiz,
# game scores, leaderboards, predictions, highlights, coupons).
VITE_API_BASE_URL=https://api-football.cancercare.pro/api

# Endpoint 2 — World Cup match data backend (fixtures + graded match results).
VITE_WORLDCUP_API_BASE_URL=https://backend.cancercareworldcup.com/api

# Endpoint 1 — Cancer Care social site: owns accounts/passwords, provides SSO.
# Used by the "Forgot Password?" info modal on the login screen.
VITE_CANCERCARE_URL=https://cancercare.pro